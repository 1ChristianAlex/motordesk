info:
  name: Client - Get self client info (GET)
  type: http
  seq: 2
  tags:
    - self

http:
  method: GET
  url: "{{baseUrl}}/self"
  auth:
    type: bearer
    token: "{{clientToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: "200"
    request:
      url: "{{baseUrl}}/self"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "data": {
              "id": 8768,
              "addressId": 6834,
              "firstName": "string",
              "lastName": "string",
              "email": "string",
              "phone": "string",
              "cpf": "string",
              "isActive": false,
              "createdAt": "string",
              "updatedAt": "string",
              "companyId": 3689
            },
            "status": 9652,
            "errors": null
          }
  - name: 400 Response
    description: "400"
    request:
      url: "{{baseUrl}}/self"
      method: GET
    response:
      status: 400
      statusText: Bad Request
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "data": {},
            "status": 9036,
            "errors": null
          }

docs: Get self client info
