info:
  name: Manager - Delete vehicle
  type: http
  seq: 4
  tags:
    - vehicles
    - delete
    - "{id}"

http:
  method: DELETE
  url: "{{baseUrl}}/vehicles/delete/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 202 Response
    description: "202"
    request:
      url: "{{baseUrl}}/vehicles/delete/:id"
      method: DELETE
      params:
        - name: id
          value: "1"
          type: path
    response:
      status: 202
      statusText: Accepted
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "data": {},
            "status": 9036,
            "errors": null
          }
  - name: 400 Response
    description: "400"
    request:
      url: "{{baseUrl}}/vehicles/delete/:id"
      method: DELETE
      params:
        - name: id
          value: "1"
          type: path
    response:
      status: 400
      statusText: Bad Request
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "data": {},
            "status": 9036,
            "errors": null
          }

docs: Virtually delete a vehicle
