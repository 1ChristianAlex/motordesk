info:
  name: /openapi/:**
  type: http
  seq: 1
  tags:
    - openapi
    - "{**}"

http:
  method: GET
  url: "{{baseUrl}}/openapi/:**"
  params:
    - name: "**"
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
