$kind: http-request
description: Get vehicle given a client data
url: "{{baseUrl}}/vehicles/owner"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{clientToken}}"
examples: .resources/Get vehicle by owner token.resources/examples
order: 1000
