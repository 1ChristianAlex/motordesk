$kind: http-request
description: Get all service order from self
url: "{{baseUrl}}/service-order/client"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Client - Get services orders.resources/examples
order: 2000
